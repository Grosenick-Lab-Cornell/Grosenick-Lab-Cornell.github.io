---
import siteConfig from "@/content/site.yaml";

const year = new Date().getFullYear();
---

<footer class="border-t border-line py-8">
  <div
    class="mx-auto max-w-[1120px] px-7 flex justify-between gap-4 flex-wrap font-mono text-xs text-faint"
  >
    <span>© {year} {siteConfig.name} · {siteConfig.institution}</span>
    <span>grosenicklab.org</span>
  </div>
</footer>
